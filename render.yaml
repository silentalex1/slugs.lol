services:
  - type: web
    name: slugs-lol-website
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    nodeVersion: 18
    envVars:
      - key: NODE_ENV
        value: production
